property {
    define {      
        a = controller.alarm && !controller.armed && door.isOpen;
        b = !controller.alarm && controller.armed && door.isOpen;
    }

    Assertion {
         safety_prop1: !a || !b;
         safety_prop2: !(a && b);
    }
}
